from django.apps import AppConfig

class VoidauthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'voidauth'
    verbose_name = 'VoidAuth Zero-Knowledge Authentication'
